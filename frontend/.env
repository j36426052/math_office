# Frontend-only environment variables (Vite).
# This file is used ONLY when running `npm run dev` inside frontend/.
# In container / production build, Dockerfile sets VITE_API_BASE=/api so this value is ignored.
# If you reverse proxy frontend and backend together, you can leave it blank to use same-origin.

# Example (dev hitting separate backend):
# VITE_API_BASE=http://localhost:8000

# Current default intentionally blank to prefer same-origin when possible.
VITE_API_BASE=
