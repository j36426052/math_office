# Vite front-end dev environment example
# Copy to .env for local development only: cp .env.example .env
# In production builds (Docker), VITE_API_BASE is set to /api inside the Dockerfile
# Leave blank for same-origin when using nginx proxy that serves both front and back.
# Example pointing to standalone backend for dev:
# VITE_API_BASE=http://localhost:8000
VITE_API_BASE=
