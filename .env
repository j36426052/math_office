# Backend
# Comma separated list of allowed browser origins for CORS
# Example for dev + deployed site:
# BACKEND_CORS_ORIGINS=http://localhost:5173,https://booking.example.edu
BACKEND_CORS_ORIGINS=

# Frontend (Vite)
# If deploying frontend separately from backend, set full API base URL.
# If blank, frontend will use same-origin calls (relative) unless running dev server port 5173.
# VITE_API_BASE=https://booking.example.edu/api
VITE_API_BASE=

# Admin Basic Auth (optional; if unset, admin endpoints are open)
# ADMIN_USER=admin
# ADMIN_PASS=change_me
ADMIN_USER=qsnake
ADMIN_PASS=jst101101101

# Database (override default sqlite ./app.db inside backend working dir)
# For docker-compose persistent volume example:
# DATABASE_URL=sqlite:////data/app.db
DATABASE_URL=

# Frontend dev separate env:
# See frontend/.env.example (copy it to frontend/.env for local Vite only)
